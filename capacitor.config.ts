import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Republic of Rome',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
